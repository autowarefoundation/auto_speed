# Export and quantize auto_speed model

In order to export auto_speed model to ONNX format use the `convert_pytorch_to_onnx.py` script

```
# python3 -p <PATH TO THE PYTORCH FILE> -o <PATH TO THE OUTPUT ONNX FILE> 
```